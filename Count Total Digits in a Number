#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int counts(int n)
{
    int count=1;
    if(n==0)
    return 0;
    else
    return count+counts(n/10);
}
int main()
{
    int n;
    cin>>n;
    cout<<counts(n);
    return 0;
}
