#include<bits/stdc++.h>
using namespace std;
bool issorted(int arr[],int n)
{
    for(int i=0;i<n;i++)
    {
        if(arr[i]>arr[i+1])
        return false;
    }
    return true;
}
int main()
{
    int arr[]={1,22,3,4,5};
    int n=5;
  cout<<issorted(arr,n);
}
