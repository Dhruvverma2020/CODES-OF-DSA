#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    unordered_set<string>x;
    for(int i=0;i<n;i++)
    {
        string s;
        cin>>s;
        x.insert(s);
    }
    int q;
    cin>>q;
    while(q--)
    {
        string y;
        cin>>y;
        if(x.find(y)==x.end())
        {
            cout<<"no"<<endl;
        }
        else
        {
            cout<<"yes"<<endl;
        }
    }
}
