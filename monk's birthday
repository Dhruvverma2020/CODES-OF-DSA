#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        set<string>x;
        for(int i=0;i<n;i++)
        {
           string s;
            cin>>s;
            x.insert(s);
        }
        for(auto name:x)
        {
            cout<<name<<endl;
        }
    }

    return 0;
}
